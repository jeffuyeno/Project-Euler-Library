if(!window.euler) {
	var euler = {
		/**
		 * Gets all of the multiples of a number
		 * @param  {Int} num
		 * @return {Array} ordered array of multiples, should have no duplicates
		 */
		getMultiples : function(num) {
			var 
		}
	};
}
